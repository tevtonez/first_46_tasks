1. Write a program that given a text file will create a new text file in which all
2. the lines from the original file are numbered from 1 to n (where n is the number
3. of lines in the file).
4. Write a program that will calculate the average word length of a text stored in
5. a file (i.e the sum of all the lengths of the word tokens in the text, divided
6. by the number of word tokens).
